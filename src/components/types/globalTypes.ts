export interface IBook {
  id: number;
  title: string;
  author: string;
  img: string;
  price: string;
  featured: boolean;
  genre: string;
  status: boolean;
  rating: number;
  dateOfPublication: string;
}
